Notification.requestPermission()

let time = 0;

setInterval(1000, function() {
    const nowDate = new Date;
    const nowTime = `${nowDate.getHours()}:${nowDate.getMinutes()}`;

    if(nowTime === todoOBJ.time) {

    }
})
